import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ProfilePicUploadScreen = () => {
  return (
    <View>
      <Text>ProfilePicUpload</Text>
    </View>
  );
};

export default ProfilePicUploadScreen;

const styles = StyleSheet.create({});
