import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const AddressInfoScreen = () => {
  return (
    <View>
      <Text>AddressInfoScreen</Text>
    </View>
  );
};

export default AddressInfoScreen;

const styles = StyleSheet.create({});
